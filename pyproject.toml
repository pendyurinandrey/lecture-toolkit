[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "lecture-toolkit"
version = "0.1.0"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["fork_join", "common", "speech_to_text_whisper", "speech_to_text_gigaam"]
