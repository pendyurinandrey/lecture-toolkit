# Единый venv для pipeline_ui.py: fork_join (stdlib, без доп. зависимостей)
# + speech_to_text_gigaam и diarization (gigaam[longform] приносит torch и pyannote.audio)
# + wakepy и PySide6 для самого pipeline_ui.py. speech_to_text_whisper НЕ входит в этот
# venv — у него конфликтующая версия torch, работает только отдельно.
gigaam[torch,longform] @ git+https://github.com/salute-developers/GigaAM.git
certifi
wakepy
PySide6
